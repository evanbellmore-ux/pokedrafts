import LeagueOverviewClient from "./LeagueOverviewClient";

/** Title comes from the league layout's generateMetadata (league name). */
export default function LeagueOverviewPage() {
  return <LeagueOverviewClient />;
}
